---
title: "CLI & Scripts"
description: "Commands and scripts useful for site authors."
pubDatetime: 2025-12-21T00:00:00Z
navLabel: "CLI & Scripts"
---

Common commands:

- `pnpm install` — install dependencies
- `pnpm dev` — run the development server with live reload
- `pnpm build` — build a production site
- `pnpm preview` — preview the built site locally

<!-- TODO: add CI/CD or deployment notes if relevant -->
