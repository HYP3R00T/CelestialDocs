---
import BreadcrumbComponent from "./BreadcrumbComponent";

interface Props {
    items: Array<{
        label: string;
        href?: string;
    }>;
}

const { items } = Astro.props;
---

<BreadcrumbComponent items={items} client:load />
